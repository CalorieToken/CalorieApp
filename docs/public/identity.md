# Public identity overview

CalorieApp uses external sign-in as identity context while maintaining its own application session. The backend validates the sign-in result through trusted server-side exchanges and binds private food logs to an internal user record.

The current identity feature does not provide wallet custody, signing, token transfers or financial execution. Security-sensitive configuration and production endpoints are intentionally excluded from public documentation.
