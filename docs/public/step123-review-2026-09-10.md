# Steps 1–3 review and showcase readiness

10 September 2026. This is a source/evidence review, not a live deployment or production-readiness certificate. It preserves the owner's acceptance of Step 1 (foundation) and Step 2 (website/app identity integration).

## Latest: owner-installed 1.4.10 and Blog/X lifecycle audit 1.4.11

The owner reports installing 1.4.10. The cloud browser again timed out before
fresh page inspection, including after one bounded fresh-tab recovery. No new
installed-version, screenshot, mobile or cross-browser pass is claimed. The
eight-page live evidence below remains dated 1.4.9 evidence.

Eight additional DOM scenarios exercise the Blog/X integration. Six reproduced
failures receive narrow corrections: a repeating observer feedback loop between
X readiness notifications and helper visibility; the missing clean `/blog/` route;
the old load state and observer surviving panel replacement; late results
disabling a newer observer; an existing CMS frame's state being watched on the
wrong panel; and a replacement anchor becoming undiscoverable after consent was
withdrawn and granted again with the shared SDK already loaded.

Readiness notifications now emit only on a changed state, and the helper does
not rewrite unchanged visibility. A real DOM MutationObserver test detects and
bounds the previously repeating queue. This does not establish the cause of the
cloud browser connection failure.

Each panel now owns its load generation and observer. Retiring a panel cancels
its local loading timer and removes only controller-owned output; CMS/CMP frames
remain intact. A render rejection ends the loading state while retaining cleanup
for late provider output. Existing profile links, eleven-language copy and
explicit CMP consent remain authoritative. No additional SDK, forced consent,
automatic retry loop, CSS/layout, login, frontend/backend or schema change is
introduced. Provider availability is not guaranteed by this repair.

The final candidate is Site Style 1.4.11. The original 27 requirements remain in
the owner audit, including separate app rollout, real Xaman/device acceptance,
unreviewed live pages, historical translations and legacy LiveCoinWatch cleanup.
No merge, deployment, wallet or ledger operation accompanies this source audit.

## Earlier: installed 1.4.9 review and bounded 1.4.10 corrections

The owner installed 1.4.9 for this review. Chrome at 1363×936 confirmed the round
Home/Whitepaper/usecase headers, readable anonymous account/app controls, shared
footer, consolidation wallet and Roadmap card/button. Eight public pages were
inspected; Richlist inspection reached the header and table structure. The
browser then failed during navigation to Blog and did not recover. It exposes
no viewport/engine switching here, and the local Android preflight found neither
ADB nor an emulator. These observations do not establish mobile, rotated,
widescreen, Safari, Firefox or physical-device acceptance.

Three concrete issues receive a narrow Site Style correction:

- Above 1200 px, move the existing shortcut stack to the right edge: its previous
  offset visibly covered CAL-guide and Trustline text. Smaller-screen geometry,
  button design, account controls and the page layout are preserved.
- Reuse the exact legacy Trustline context note instead of inserting a second
  copy with the same ID. Keep the existing link and handlers; restore only still
  managed text on cleanup and preserve custom edits/markup.
- Follow late page-height growth for at most 1.8 seconds after an explicit
  bottom-button click. Manual wheel/touch/pointer/keyboard input, page exit or
  hiding cancels the adjustment. The original click handler is retained.

The four added regression cases failed before these fixes and pass afterwards.
The existing isolated identity simulation passed 176 backend cases and 28
interface/integration cases without a real XRPL account, network signature,
ledger submission or production data. It is mock identity-flow evidence, not
an Android/Xaman or ledger test. The old LiveCoinWatch loader also emitted
background console errors while XPMarket displayed data; its cause is recorded
without attributing the browser failure to it or changing Identity Bridge.

No intermediate owner debugging round is required. Later entries below retain
the earlier implementation history; current live observations are those above.

## What stays stable

| Step | Existing foundation | Evidence boundary |
| --- | --- | --- |
| 1 — app and ecosystem | PostgreSQL-only live configuration; forward migrations; owner-scoped private history; bounded requests/storage; source-independent food assertions; export/erasure and recovery contracts | Required CI exercises synthetic PostgreSQL migration, isolation, restart, backup/restore and related guards. This does not establish the current production provider configuration. |
| 2 — integration | Existing same-tab Xaman login, joint session refresh and logout, exact-origin iframe messages, first-party proxy cookies | Previously accepted by the owner; current auth/UI regressions remain required. The identity protocol, database schema and Identity Bridge plugin remain unchanged. The 1.4.8 follow-up adds an explicit read-only barcode search mode to the existing backend food route. |
| 3 — presentation | Historical artwork, shared site chrome, external CAL/XRP routes, original wallet links, translated UI, FAQ help and mobile controls | Site Style 1.4.5 was observed live at this audit. Later source changes require plugin installation and owner acceptance. |

The older baseline and synthetic-provider evidence files describe dated observations. Their historical statements must not be read as a current live deployment status. No new provider operation, migration, credential handling, transaction or deployment is authorized by a styling release.

## Additional automated audit: Site Style 1.4.9

The owner requested fewer manual test/debug cycles. This round reproduces and
fixes additional integration failures while keeping the accepted identity
protocol, database schemas, artwork, CSS layout and direct production dependency versions
unchanged:

- Reattach the existing app/language widget after native account-footer replacement.
- Rebase public translation records after a CMS text replacement, preserving the
  new English content and unknown custom edits.
- Exclude Home previews consistently on the server.
- Ignore stale single/bulk deletion responses after logout or unmount; an old
  authorization failure cannot clear a newly authenticated diary.
- Isolate malformed food records and exclude results that would fail the existing
  diary schema. Boolean values are unknown nutrition, not measured zero/one.
- Reset empty-query search flags, addressing the later automated review note.
- Use the valid Render commit as build metadata when no explicit release ID is
  configured. The observed live app reported `development`; explicit operator
  IDs retain priority. This uses Render's [documented public build variable](https://render.com/docs/environment-variables),
  with no dashboard access or configuration change.

CI now discovers every `tools/tests/*.test.mjs` file instead of relying on a
manually maintained list. A development-only DOM dependency supports interaction
tests of the actual WordPress scripts; it is not shipped in the plugin or app
bundle. Additional scanner UI and actual ZXing raster-decoding tests cover all
four advertised formats. No new direct production package version, provider request,
paid service, signing action or deployment is introduced.

The broader dependency audit also found eleven findings in pre-existing
development tools. ESLint's Next configuration is now aligned with Next 15.5.25
and affected development dependencies receive compatible security fixes. The direct production packages remain unchanged; the shared caniuse-lite
browser data also receives a compatible update. The resulting complete npm audit reports
zero known vulnerabilities; this is a dependency check, not a security certification.

Live Home was rechecked and still loaded Site Style 1.4.5; the public app lacked
the new scanner. The review requires no intermediate owner testing. Source/CI
completion remains distinct from final installed layout, camera hardware,
historical content coverage and deployed build verification. The outstanding
Step 3 requirements below are retained rather than silently signed off.

## Included: Site Style 1.4.8 and food barcode scanning

All earlier corrections below remain included. A deeper CSS review found that
the native header's 173 px column left a smaller logo than the fallback header's
173 px image. The native grid now allocates the same logo/padding/menu space.
Native and fallback mobile headers share a breakpoint, rounded surface, logo
scale and a menu that expands in document flow. Account content can wrap inside
its natural height, with the original 65 px avatar size. Footer links and cookie
controls share typography and touch-target height. Known current-page menu links
receive an accessible marker; existing link destinations and event handlers remain.

CAL & Crypto adds a short wallet → route → wallet-review sequence and links to
CalorieApp and the whitepaper. External CAL/XRP providers remain identifiable.
The operator's request conditions an own DEX on established legal permissibility.
The [Commission answer via ESMA, 21 May 2026](https://www.esma.europa.eu/print/pdf/node/222469)
leaves the fully decentralized/intermediary assessment to the authority case by
case; [MiCA article 59](https://www.esma.europa.eu/publications-and-data/interactive-single-rulebook/mica/article-59-authorisation)
requires the applicable authorization for qualifying services. This review has
no new authorization or operator-specific exemption evidence. Our inference is
that permission for the proposed operator-managed order/signing interface is
not established. Consequently no own order execution, routing, AMM deposit,
CAL/RLUSD pair, wallet-signing payload or fee collection is activated. External
links are not a claim that every provider or promotion is cleared worldwide.

The separately deployed app gains an explicit camera/manual food-barcode entry,
local decoding, exact OFF product lookup, source identity checks, and cancellation
that releases even late camera grants. A one-minute timeout and search/provider
guards bound activity. No automatic diary save occurs. See
[barcode scope and device acceptance](../BARCODE_SCANNING.md).

The existing three-food USDA reference gains an adjustable edible weight with
original-precision scaling and a reset. Source hashes, unknown/censored-value
handling and diary separation are preserved. See [USDA evidence](../USDA_REFERENCE.md).
All new first-party controls/copy cover the same 11 languages. Historical CMS/legal
translation coverage remains the earlier, explicitly incomplete inventory.

Validation includes camera lifecycle/GTIN tests, actual search/USDA component
checks, exact-lookup backend mocks, unchanged name-search regressions, package
checks, frontend type/lint/build, dependency audit and the required CI on PR140.
Real camera hardware, the candidate's installed desktop/mobile layout, full
historical content and current hosting policies still require live acceptance.

## Earlier consolidated Site Style 1.4.7

All 1.4.6 screenshot corrections remain included: round headers and mobile hamburger controls; ordered CAL & Crypto/Trustline content; the consolidation wallet's grey/green/dark-button presentation; roadmap card and text link; wrapping account text; correct X iframe sizing/readiness and explicit consent; house-style donation notices; and a question-mark CalorieHelp icon.

The audit adds:

- Native menu state and Escape-to-close behavior while preserving existing nodes and handlers.
- Removal of the stale XUMM DEX tooltip on the already-corrected CAL & Crypto link.
- Translation of standalone donation notices, preserved locale during late updates, and no full public-text rescan for excluded account changes.
- Server-side exclusion of preview/editor requests from public styling.
- A read-only administrator review inventory under Tools → CalorieToken review. It lists pages/posts, publication states and candidate legacy references without changing content. The optional export includes unpublished titles and belongs in private review records. It does not inspect Brizy metadata or prove visual/link/legal correctness.
- A deterministic Site Style release builder with an explicit file inventory, matching plugin/readme versions, exact ZIP-byte verification and CI checks. The separate Identity Bridge builder remains unchanged.

The Nutri-Score colors, app translations and search preparation in PR140 deploy with the frontend. The WordPress ZIP cannot deploy them. The audit follow-up narrows the app message translator to known status/error strings; ordinary button-label text and unknown provider messages pass through. Authentication requests and state transitions are unchanged.

## Readiness for Step 4

| Showcase subject | Suitable claim after the chosen live journey passes | Still excluded or pending |
| --- | --- | --- |
| Food search and diary | Demonstrate actual search, product nutrition, portions and the user's diary | Cold-start availability must be checked on the deployed version; no guaranteed availability or invented average Nutri-Score |
| Identity | Demonstrate the accepted same-tab sign-in/sign-out journey | Device acceptance of the final deployed combination; no custody/payment capability claim |
| Languages | Demonstrate the eleven-language app/interface controls that work in the selected screens | Historical CMS/blog/legal/image text is not complete in eleven languages |
| USDA | Show the three dated, attributed reference foods | No broad USDA search or live USDA integration claim |
| Website | Show the accepted desktop/mobile pages and original visual identity | New ZIP requires owner visual acceptance; unpublished/hidden pages are not all reviewed |
| CAL & Crypto / Trustline | Show the website's external provider links and instructions | No own trading engine, executed order or completed trustline claim without separate evidence |
| Pilot / DAO / Testnet | Explain the documented future direction and existing guide | Retailer membership, consumer payment requests, test-CAL distribution and active DAO voting are not implemented/accepted by this release |

Step 4 draft planning can use this evidence map. Final eleven-language showcase acceptance and Step 5 publication remain behind their unfinished content, deployment and human-review requirements. A successful ZIP build does not close those requirements.

## Repeatable release workflow

1. Start from the current reviewed PR head and list the bounded changes. Preserve the backend/Identity Bridge baseline for a styling-only round.
2. Run changed-feature tests and the Site Style package builder locally. Use the existing CI once the complete batch is ready; address concrete findings rather than repeatedly rebuilding unchanged parts.
3. Build `python tools/build_site_style_release.py --output-dir <release-directory>`. Keep the generated archive, version, SHA-256 and source manifest together.
4. Record one source head and its CI status, then deliver one replacement ZIP. Distinguish source, merged, deployed and human-accepted states.
5. Run one grouped desktop/phone acceptance pass, including logged-in/out and X consent states. Use the administrator inventory to track additional pages instead of silently assuming whole-site coverage.
6. Preserve the Step 1–3 outstanding items when preparing Step 4. Do not mark plans or historical claims as demonstrated features.

[PR140](https://github.com/CalorieToken/CalorieApp/pull/140) · [Release gates](release-readiness.md) · [September source scope](website-update-2026-09.md)
